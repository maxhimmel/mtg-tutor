node_modules/
dist/
.next/
next-env.d.ts
*.log
.DS_Store
.mtg-tutor/
scratch/
.omc/
.turbo/
.env
.env.local
*.tsbuildinfo

# Raw 17Lands datasets. Build inputs only -- ~1.2GB per set, streamed and
# reduced to a ~270KB artifact under packages/backend/data, which IS committed.
# Caching them here just saves re-downloading while tuning metric definitions.
datasets/

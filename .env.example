# Without ANTHROPIC_API_KEY the draft still runs, using deterministic feedback.
ANTHROPIC_API_KEY=

# Optional — override the coaching model (default: claude-sonnet-5).
# ANTHROPIC_MODEL=claude-sonnet-5
